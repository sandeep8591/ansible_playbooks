---
  - name:  Identifying and modifying bond0
    import_tasks: ens33.yml
    when:  ansible_ens33.device == "ens33"
    #- action: debug msg="ethernet = {{  ansible_enp0s3.device }}"
    #- action: debug msg1="IPAddress = {{  ipv4.address }}"
